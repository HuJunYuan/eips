@model EIP.Entity.College

@{
    Layout = null;
}
@section head{
}
<form id="form_College" class="easyui-form" method="post" data-options="">

    <!--学院信息表编号-->
    @Html.HiddenFor(p => p.CId)


    <div class="form-item">
        @Html.CLTextBoxFor(p => p.CollegeId, new TextBox() { Label = "学院编号 ：", Style = "width:90%;" })
    </div>

    <div class="form-item">
        @Html.CLTextBoxFor(p => p.CollegeName, new TextBox() { Label = "学院名 ：", Style = "width:90%;" })
    </div>


    <div class="form-item">
        @Html.CLTextBoxFor(p => p.CollegeAddress, new TextBox() { Label = "学院地址 ：", Style = "width:90%;" })
    </div>


    <div class="form-item">
        @Html.CLTextBoxFor(p => p.Other, new TextBox() { Label = "备注 ：", Style = "width:90%;" })
    </div>

</form>
<script type="text/javascript">

    function validate() {
        // 验证
        var isValid = $("#form_College").form("validate");
        if (!isValid) {
            App.showMessageBox('@MessageManager.GetMessage("E10060")', 2);
            return false;
        }
        return true;
    }

    //保存
    function save() {

        // 表单验证
        if (!validate()) return;

        // 表单数据提交
        var formData = $("#form_College").form("getData");
        App.action(App.getUrl("/College/save"), formData,
            function (data) {
                //todo
            });

    }

    //关闭
    function closeBox() {

        window.App.showConfirm('@MessageManager.GetMessage("Q10030")',
            function () {
                window.App.CloseWindow();
            });
    }
</script>
